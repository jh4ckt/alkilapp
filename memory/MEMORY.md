# Memory Index

- [AI Game Assistant](ai_game_assistant.md) — standalone executable AI for PC video games (overlay + coach + autocast, Gemini via OpenAI-compat endpoint); separate project from the stickman repo
- [AlkilMouse: control de mouse PC](pc_mouse_control_alkilmouse.md) — standalone PowerShell program (localhost HTTP + consola): mueve/clickea/scroll/draga el mouse, escribe texto, captura pantalla; límites de seguridad (token, clamping, sin teclas peligrosas, audit log)
- [Android Google Maps + inmuebles](android_google_maps_inmuebles.md) — AlkilApp: repo público https://github.com/jh4ckt/alkilapp, local D:\alkilapp, v1 compilada+instalada en dispositivo; **API de Maps real configurada** (proyecto GCP `gen-lang-client-0040505884`, clave restringida a com.alkilapp); gaps (Places SDK y datos reales); **reseñas al propietario 1-5 (commit 0bb0fcd)** + menú/filtros fusionados, "Ver ubicación en mapa"
- [AlkilApp: base Firestore usuarios+propiedades](android_alkilapp_firestore_base.md) — base de datos en `alkilappdb` (colecciones `usuarios` y `propiedades` + reglas de seguridad + alerta de presupuesto); modo gratuito garantizado dentro de cuota free (billing activo es obligatorio para Maps, pero crédito $200/mes lo cubre). **2026-09-14: TODO lo que fallaba al desplegar quedó arreglado** (release de reglas POR BASE, Auth provisionado, app Android cableada y andando en vivo). Ronda 2: moneda (PEN/USD), filtros departamento/distrito (Lima), ciudad Lima al publicar, demo reseed a Lima. **Google Sign-In CONFIRMADO END-TO-END (2026-09-15)**: el padre firmó en su Xiaomi con `presupuestos@oleohidraulic.com` (uid `smpJfxQT0vUMaCMalJWUej1qPnw1`); `usuarios` tiene 2 docs (email-uid + google-uid); SHA-1 debug `b4ed9dc4…` = el registrado en Firebase. Ronda 3 (también confirmada en vivo): subida de fotos (probado con 2 fotos, base64 en `fotos`) + lista ordenada por cercanía (haversine, distancias reales del teléfono) + alta como usuario Google. **NOTA Xiaomi 24116RACCG**: aún con "Depuración USB (Ajustes de seguridad)" en ON, `adb input` da `SecurityException: INJECT_EVENTS` → el padre toca, el agente lee (uiautomator). **Backend + seed (2026-09-15)**: primer backend Node en `D:\alkilapp\backend` (`seed.js` con @google-cloud/firestore, SA `alkilapp-seed`); esquema spec nuevo (users/listings/reviews/chats) **fusionado** sobre las colecciones que ya lee la app (`usuarios`/`propiedades`) + `chats`/`reviews` nuevos; rules v2 (`7097e229…`) cubre chats/reviews/usuarios-por-uid (api Rules: no hay update, solo DELETE+POST). **MÓDULO CHAT NATIVO (2026-09-15)**: bandeja (`ChatListActivity`) + conversación (`ChatDetailActivity`) compiladas e instaladas en el Xiaomi; seed con 2 chats reales del padre (`chat-papa-renzo`, `chat-papa-diego`, uid suyo como participante) para que le aparezcan; rules v3 (`50640439…`) permite crear chats desde la app (request.resource.participants). **Destacar + Verificación (misma sesión)**: `isFeatured`/`featuredUntil` en propiedades, `trustLevel`/`verificationBadge` en usuarios; tarjeta con borde dorado + etiqueta "Destacado" (destacados primero) y chip "Verificado por Alkilapp"; escudo verificado en chat y tarjeta; `backend/queries.js` (`obtenerPropiedadesActivasLima`, destacadas primero, `npm run query-demo`). **FICHA DEL INMUEBLE + TAREA A/B (2026-09-15)**: `PropiedadDetalleActivity` nueva — galería (base64 `fotos` + URLs `photos` con HttpURLConnection+Executor/Handler, sin librería), info, botón coral "Chatear con el Propietario" que crea `chats/inm-<listingId>` determinístico, tarjeta propietario con verificado/rating; click en tarjeta abre la ficha; `estado != "under_review"` oculto. Backend: `verificacion.js` (TAREA A: código 6 dígitos, `verifications/{email}`, 10 min, → `emailVerified/trustLevel='basic'/verificationBadge`, demo OK con Valeria) y `moderacion.js` (TAREA B: `reports` + transacción reportsCount→3⇒`under_review`, demo OK con seed-l-depa-sanmiguel). Rules v4/v5 (v4 = `d605869a…` reports+verifications; v5 = `a220c618…` chats legibles aun inexistentes = fix del "chat que no abría", root cause: leer `chats/inm-<id>` inexistente daba PERMISSION_DENIED por `resource.data` null, no era bug de UI) en los 2 releases. **Ronda correcciones (misma sesión)**: chat abre con set-merge (sin get previo), botón "Denunciar anuncio" (dialog_denuncia con motivos + detalle → `reports`), perfil del propietario (`PerfilPropietarioActivity`: foto/rol/verificado+trustLevel/rating/contacto/reseñas en vivo + botón chatear). Front aún no consume reviews/features/location salvo reseñas en el perfil (galería y chat ya sí). **Ronda mapas (2026-09-15, SIN probar aún en vivo)**: filtro distrito mueve el mapa a la zona (marcadores + LatLngBounds, `adapter.visibles()`), FAB "mi ubicación" vuelve a centrar al usuario, alta ya NO es pop-up sino `RegistrarPropiedadActivity` (formulario fullscreen, ubicación = posición actual, pasada por extras + refresh con FusedLocation), tipo `habitacion` agregado a `tipos_inmueble`. **Ronda visor de fotos + Mi perfil (2026-09-15, en vivo)**: fotos abren a pantalla completa con zoom (`FotoZoomActivity` + ViewPager2 + `ui/ZoomImageView.kt` custom sin librerías: pinch/doble-tap/pan, resolución completa; dep `viewpager2:1.1.0`; **crash "Pages must fill the whole ViewPager2" encontrado por el padre al tocar una foto → fix: `RecyclerView.LayoutParams(MATCH_PARENT,MATCH_PARENT)` explícito**); cabecera con sesión muestra "Mi perfil" (diálogo Mi perfil/Cerrar sesión); `MiPerfilActivity` con foto/name/tel/tipo cuenta (set merge) + `guardarUsuarioEnBase()` ahora get-then-merge sin pisar perfil; foto base64 `fotoBase64` fallback en perfil propietario. **Ronda correcciones en vivo**: "Mi perfil" ahora abre directo el formulario (sin pop-up con correo; padre se quejó), "Cerrar sesión" dentro del perfil; formulario de alta ganó habitaciones/m² (`etPropAmbientes`/`etPropSuperficie`) + comodidades (ChipGroup con array `comodidades_disponibles`) guardadas y mostradas en la ficha. **Autocomplete de direcciones con Google Maps (same 2026-09-15)**: dependencia Places SDK 3.5.0 + `App.kt` (Places.initialize con la clave del Manifest; la clave AlkilApp Android ya tenía places-backend habilitado, sin tocar GCP); campo "Buscar dirección en Google Maps" en el alta → sugerencias (debounce 450ms, Perú via RectangularBounds+setCountries) → al elegir rellena dirección + coord. precisas (lat/lng del lugar). **iOS de AlkilApp: PENDIENTE (solicitado por el padre).** **Ronda 4 pedidos en vivo (2026-09-15)**: (1) letra más oscura (`text_secondary #6B7280→#374151`); (2) el perfil del propietario lista sus **Inmuebles publicados** (query `idPropietario==uid`, chips de estado Disponible/Finalizado/En revisión, tap→ficha con extras completos); (3) "Dar por finalizada la publicación" en la ficha + "Ya lo alquilé" en el perfil (update `estado="finalizado"`, banner en la ficha, oculto del feed `estado != "under_review" && != "finalizado"`); (4) FAB "mi ubicación" sube con el bottomSheet (`BottomSheetBehavior.onSlide` → translationY = -slideOffset*250dp; `slideOffset` no es getter público aquí → arranque con `sheet.state==STATE_EXPANDED`). **Rules v6 (`bae8ad21…`)**: `propiedades.update/delete` SOLO del dueño (`request.auth.uid==resource.data.idPropietario`); desplegadas con SA tras IAM `roles/firebaserules.admin`; PATCH de release no funciona → DELETE + POST con `name` completo en el body. **En vivo**: el padre finalizó todas sus publicaciones reales (Cuarto g8dDra…, 1cfZrA…, 57fp6…, Ctxe…) → quedaron `finalizado` en `alkilappdb` y off del feed (no se resetean sin que él pida). iOS AlkilApp sigue pendiente. Pendiente: push a GitHub (sin confirmar). **Ronda 5 (2026-09-17, instalado en el Xiaomi, sin probar aún por el padre)**: crash del feed RESUELTO (`TransactionTooLargeException` por fotos base64 en el Intent → `PropiedadDetalleActivity` ahora carga fotos de Firestore por id); design system Tailwind aplicado a todo el app Android (tokens en `colors.xml` con alias de compatibilidad → coral/slate en toda la app, Property Card nueva con favorito/badge/detalles/CTA, destacado en dorado `#D4A017`); orden del feed = tier (destacado → verificado → sin verificar) y luego cercanía; **login/registro nuevo** `AuthActivity` (tabs, nombre, repetir contraseña, errores inline, Google, olvidé contraseña) reemplaza el dialog; **verificación de identidad con documento** `VerificacionActivity` → `verificaciones/{uid}` (base64 ~1000px) + resumen en `usuarios/{uid}.verification`; **panel admin desplegado en Cloud Run**: https://alkilapp-admin-288429280561.us-central1.run.app (`backend/admin/server.js`, Node puro, login `ADMIN_PASSWORD`; aprueba verificaciones/publicaciones/denuncias); rules **v7** (`8852e34c…`) agregan `verificaciones/{uid}` y ahora las reglas viven en el repo (`backend/firestore.rules` + `backend/reglas.js` con `show`/`deploy`). **Ronda 6 (2026-09-17, instalado, sin probar por el padre)**: miniaturas del listado ya no pixeladas (`decodificarThumb` a `dp(420)` con dos pasadas, antes 144px fijos); crash del visor de fotos arreglado (`FotoZoomActivity` carga por id de Firestore, `inSampleSize` a 2048, decode en background — nunca más base64 por Intent); `Propiedad.estadoNormalizado` (`""`/`"publicado"`/`"activo"` → `"disponible"`) en badge/ficha/perfil; panel admin con botón **Eliminar** (con confirm), aprobar → `"disponible"` (antes `"publicado"`) y destacar por la duración pedida; destacar **7/15/30 días** desde el app (`destacadoDias`, diálogo en `solicitarDestacar`) y `backend/destacar.js` respeta esos días; nuevo `backend/expirar.js` + `GET /cron/expirar?clave=<ADMIN_SECRET>` (antes del gate de sesión) con **Cloud Scheduler** `alkilapp-expirar-destacados` (us-central1, `0 3 * * *` America/Lima, disparo verificado en logs); panel en revisión `alkilapp-admin-00005-f7v`.
- **Tercer lote 2026-09-17 (instalado, sin probar por el padre)**: popup del mapa cableado de VERDAD — `construirInfoWindow` era código muerto, no había `setInfoWindowAdapter`; ahora muestra tipo normalizado (Habitacion/Departamento/Casa/Otros) + precio + zona, click abre ficha; pantalla estática nueva `MisPublicacionesActivity` (estilo RegistrarPropiedad, no popup) y el drawer "Ver mis publicaciones" la abre (antes PerfilPropietario); "Mi perfil" sin las 2 opciones de publicaciones/verificación.
- **Ronda 7 (2026-09-17, APK instalado, panel admin redepleagado — SIN probar por el padre)**: (1) admin "Aprobar" → `estado="publicado"` (antes "disponible"; la app lo lee como disponible en el feed) + nuevo selector **"Cambiar"** de estado por publicación (`publicado/disponible/finalizado/under_review/pendiente`, endpoint `/publicaciones/{id}/estado`, marca `estadoCambiadoAdmin`), Cloud Run redeployed rev `alkilapp-admin-00006-6ft`; (2) **notificaciones de chat nuevo SIN FCM** — nuevo `ChatNotificaciones.kt`: canal `alkilapp_chats`, `MonitorChats` (App.onCreate + AuthStateListener) con snapshot `chats {participants:me}`, primera lectura siembra sin avisar (evita spam de no-leídos viejos), avisa solo si `unreadCount[me]>0 && lastMessageAt>previo` y no estás viendo el chat/bandeja (`ChatVista`), tap abre `ChatDetailActivity`; `POST_NOTIFICATIONS` en Manifest + request en MainActivity (SDK 33+, dialogo al abrir la app — el padre debe tocar Permitir); limitación: con la app matada no llegan (FCM queda como pendiente opcional); (3) chats por inmueble confirmado de fábrica: determinístico `chats/inm-<listingId>` con listingTitle en bandeja y cabecera del chat → varios alquileres = conversaciones separadas.
- **Ronda 8 (2026-09-17, instalado, sin probar)**: filtros del mapa ahora también filtran por **tipo de inmueble** (departamento/casa/habitacion/otro) y **cantidad de habitaciones** (1/2/3/4+); `setFiltros(dep, dis, tipo, hab)` con `tipoClave()` normalizador; campos `filtroTipo`/`filtroHabitaciones` en MainActivity + strings/array `opciones_habitaciones`.
- **Ronda 9 (2026-09-18, compilado+instalado en el Xiaomi, admin redeployed — SIN probar por el padre)**: (a) **Filtros → Bottom Sheet compacto** — `MaterialBottomSheetDialog` + `bottom_sheet_filtros.xml` (compacto, rounded top 24dp, handle pill, Tipo+Habitaciones en 2 columnas, botones Limpiar/Aplicar coral); eliminado `dialog_filtros.xml`. (b) **Panel Admin — columnas de fecha** en Publicaciones (Publicado), Denuncias (Creado), Verificaciones (fallback createTime), Usuarios (Alta); helpers `mostrarCreado/ponerCreado` con fallback a Firestore `createTime`; deploy Cloud Run rev `alkilapp-admin-00007-bj7`. Build OK, APK instalado, admin redeployed; pendiente prueba en vivo del padre.
- **Ronda 10 (2026-09-18)**: **MisPublicaciones menú opciones** (editar/eliminar/suspender/reactivar/destacar) + **RegistrarPropiedadActivity modo edición** (prefill + update) + **Admin search** en 4 vistas (Verificaciones, Publicaciones, Denuncias, Usuarios con input `q`). Deploy admin rev `alkilapp-admin-00008-jgw`. Android v1.23 (vc5), release GitHub `v1.23`.
- **Ronda 11 (2026-09-18, compilado+instalado en Xiaomi — SIN probar por el padre)**: **Fix edit mode** (keys coinciden `edit_mode`/`propiedad_id`), **Destacar dialog** 3 opciones (7/15/30 dias, S/ 6.90/12.90/24.90), **Ubicación** permiso dialog al primer inicio (SharedPreferences), **Foto perfil** visible en PerfilPropietarioActivity y MiPerfilActivity. Android v1.24 (vc6), release `v1.24`.
- **Ronda 20 (2026-09-19)**: seed con **10 inmuebles ficticios en 10 distritos de Lima** (direcciones inventadas, `estado=disponible`, 3 destacados, dueño seed nuevo `u-propietario-2` sin verificar → se visualiza el tier "sin verificar"); **la tarjeta del listado lee SOLO `fotos` base64** (las URLs `photos` no salen en el card) → helper `solidPngBase64(hex)` en seed.js genera PNG 4x4 sin deps y `buildListing` acepta `fotoColor` (foto pastel por inmueble). Seed ejecutado (14 docs esDemo), app sin reinstall (feed en vivo), commit `35e17e3` pusheado, sin release. Pendiente: confirmación visual del padre. Gotcha: `adb kill-server` mata el shell → usar `adb reconnect offline`.
- **Ronda 21 (2026-09-19, instalado en el Xiaomi)**: (1) **Firma "AlkilApp by Jh4ckT"**: string `nav_footer` (pie del menú hamburguesa) + nuevo `auth_firma` en el login (bajo el texto legal). (2) **Foto de perfil en la cabecera del menú hamburguesa**: `panel_menu.xml` ahora tiene `ivNavFoto` (ShapeableImageView circular con `AlkilAppCircle`, scaleType centerCrop) sobre el `tvNavAvatar` de iniciales (GONE cuando hay foto); MainActivity `cargarFotoNav()` lee `fotoBase64` (Base64.NO_WRAP + BitmapFactory, background executor) o `profilePicture` (HttpURLConnection) de `usuarios/{uid}` y la pone; verificado en vivo: `tvNavAvatar` desaparece y `ivNavFoto` visible con la foto del padre. (3) **Badge de chats sin leer** en la fila "Mis chats" del menú: nuevo `object ChatNoLeidos` (total @Volatile + `suscribir/desuscribir` con CopyOnWriteArrayList) alimentado por `MonitorChats` en cada snapshot (`ChatNoLeidos.actualizar(chats.sumOf{it.unreadMio})`, 0 al desconectar); MainActivity se suscribe en onCreate (callback→`actualizarBadgeChats`) y refresca en `onStart`/`actualizarUiSesion`; `tvNavChatBadge` en panel_menu con `bg_badge_no_leido` (oculto en 0). En vivo: badge GONE con 0 no-leídos, correcto. (4) **Logo launcher pin-casa**: `ic_launcher_foreground.xml` → pin de ubicación cuya parte superior es un techo de casa con ojo de cerradura, `fillColor=@color/alkil_coral` (#FF6B5E), `fillType=evenOdd` (anilla ojo + ranura puntean el navy); `ic_launcher_background #0B2447` (azul marino, antes #1E293B). Android **v1.34 (vc16)**, commit `8b90844`, release `v1.34`, instalada. Nota verificacion: `adb input tap` SÍ funciona ahora (abrió drawer y MiPerfil); pixel-checks del icono en lanzador MiUI no concluyentes (caché del launcher, force-stop launcher no bastó) → confirmación visual del padre pendiente.
- **Ronda 22 (2026-09-19, instalado y VERIFICADO por píxeles)**: el padre pidió que el logo launcher "no se veía bien" → **rediseño: techo de casa con chimenea SEPARADO del pin de ubicación** (3 `<path>` independientes en `ic_launcher_foreground.xml`: chimenea+cap arriba, techo trapezoidal, pin teardrop con ojo de cerradura debajo, sin evenOdd compartido para no puntear; todo `fillColor=@color/alkil_coral #FF6B5E` sobre navy `#0B2447`). La caché del launcher MiUI fue la trampa de la Ronda 21: tras `adb install -r` + `am force-stop com.miui.home` el icono SÍ renderiza (pixel-scan exacto #FF6B5E en 2 celdas; distribución vertical: techo→gap→pin correcta). v1.35 (vc17), commit `a83d8f4`, release `v1.35`, instalada.
- **Ronda 22b (2026-09-19, instalado + verificado)**: el padre NO agradó el pin-casa → **revertir al logo ORIGINAL (casa) cambiando SOLO la paleta** a coral `#FF6B5E` sobre navy `#0B2447` (`ic_launcher_foreground.xml` = path original `M54,32 L30,56 L34,56 L34,76 L74,76 L74,56 L78,56 Z M44,76 L44,62 L64,62 L64,76 Z`; background ya era `#0B2447`). Pixel-verificación en leña: navy=4581, coral=1320, un solo cluster vertical 722..818 (la forma de casa). v1.36 (vc18), commit en el push del release `v1.36`, instalada. Gotcha: la caché del launcher MiUI otra vez (force-stop com.miui.home antes de pixel-check).
- **Ronda 23 (2026-09-19, instalado + verificado en vivo + reglas v8)**: **si una publicación quedó `finalizado`/alquilada, cualquier chat relacionado queda CERRADO para el solicitante**: `ChatDetailActivity` deriva `listingId` de `EXTRA_LISTING_ID` (fallback `chatId.removePrefix("inm-")`), `vigilarEstadoInmueble()` = snapshot de `propiedades/{listingId}` → si `estado=="finalizado"`: banner coral `cardChatCerrado` con string `chat_cerrado_inmueble` ("El inmueble ya está en alquiler, no se permite comunicación con el dueño del inmueble.") + `etEntrada`/`btnEnviar` deshabilitados + guard en `enviarMensaje()` (`if(!binding.etEntrada.isEnabled) return`); layout nuevo `ic_lock.xml` (candado material). `ChatListActivity` ahora pasa `EXTRA_LISTING_ID` al abrir. **Reglas Firestore v8** (`backend/firestore.rules`): función `inmuebleCerrado()` en `chats/{chat}/messages` (lee chat→listingId→propiedad, y si `estado=='finalizado'` bloquea CREATE de mensajes; sin `.lower()` — NO existe en Security Rules, INVALID_ARGUMENT en el deploy). Deploy con `node reglas.js deploy firestore.rules` → ruleset `5c110653…`. Verificado en vivo: abrí `chat-papa-renzo` (listing `seed-l-hab-miraflores` marcado finalizado temporalmente con SA) → banner visible `[136,1955][1018,2037]` + EditText `enabled="false"`; luego revertido a `disponible`. v1.37 (vc19), commit `fcaf080`, release `v1.37`, instalada.
- **Ronda 24 (2026-09-19, instalado + admin redeployed v00012-w7g)**: (1) **Registro con celular obligatorio**: campo `Celular` en `AuthActivity` (solo modo registro), validación ≥9 dígitos, se guarda en `usuarios/{uid}.telefono`. (2) **Mi Perfil: nombre y correo no editables** (datos únicos): layout `activity_mi_perfil.xml` cambia `etPerfilNombre` por label + `tvPerfilNombre` (solo lectura, igual que email); nota `perfil_datos_fijos`; `guardarPerfil()` elimina `nombre` del mapa, solo guarda teléfono/tipo. (3) **Verificación 2 fotos (frente + adverso)**: `activity_verificacion.xml` + `VerificacionActivity` con dos pickers/launchers; valida ambos; guarda `imagen` e `imagenReverso` en `verificaciones/{uid}`; reglas `create` exigen ambos (≤900KB). (4) **Admin panel filtros + enlaces**: cada vista (`verificaciones`, `publicaciones`, `reportes`, `usuarios`) con búsqueda + `<select>` filtros + botón Limpiar; `vistaResumen` tarjetas como `<a class="stat-link">` clicables; sesiones stateless (cookie HMAC + TTL 24h, sin `sesiones` Set). Reglas v9 (`e8b5deb4`). v1.38 (vc20), commit `6ca45ba`, release `v1.38`, instalada.
- **Ronda 11b (2026-09-18, misma sesión)**: **Fix destacar dialog** (setItems en lugar de setSingleChoiceItems), **Pausar/Marcar alquilado** estados consistentes (MisPublicaciones, Detalle, Perfil). Pausar→pausada (reactivable por usuario), Marcar alquilado→finalizado, Admin under_review no reactivable. Android v1.25 (vc7), release `v1.25`.
- **Ronda 12 (2026-09-18)**: **Billing integration** (Play Billing library + BillingManager para 3 SKUs), **Eliminar fotos en edición** (botón delete overlay en miniaturas), **Precios destacar unificados** (7d S/6.90, 15d S/12.90, 30d S/24.90 en MisPublicaciones y Perfil). Android v1.26 (vc8), release `v1.26`.
- **Ronda 25 (2026-09-20, instalado en Xiaomi HGR4RJCK)**: **Registro nacional** — 25 departamentos en `strings.xml` + spinners dinámicos depto/distrito en `RegistrarPropiedadActivity` y `MainActivity` (filtros). Para no-Lima muestra "Sin distrito" + "Otro". **Botón compartir** en `PropiedadDetalleActivity`: genera link `https://alkilapp.com/propiedad/{id}` y abre share sheet. **Fix chat**: orden consistente `participants` (`.sorted()`) en `PropiedadDetalleActivity` y `PerfilPropietarioActivity` → evita `PERMISSION_DENIED`. v1.39 (vc21), commit `c4f29e7`, release `v1.39`.
- **Ronda 26 (2026-09-20)**: **Ciudades por departamento (25 deptos)** — `strings.xml` agrega 25 arrays `ciudades_<departamento>` con capitales y ciudades principales. `RegistrarPropiedadActivity` y `MainActivity` (filtros): spinner Departamento con listener que actualiza spinner Ciudad dinámicamente (Lima → 43 distritos; otros → capitales + ciudades principales). String `filtros_scope_info` actualizado. **Fix chat**: revertido `.sorted()` en array `participants` (PropiedadDetalleActivity + PerfilPropietarioActivity) → mantiene orden original al crear chat, evita `PERMISSION_DENIED`. v1.40 (vc22), commit `beb9d85`, release `v1.40`.
- **Ronda 27 (2026-09-20)**: **Deep links** — `alkilapp://propiedad/{id}` abre `PropiedadDetalleActivity` directamente; intent-filter en manifest; botón compartir genera `alkilapp://propiedad/{id}`. **Fix MisPublicaciones detalle**: removido `EXTRA_FOTOS` (base64) del intent → evita `TransactionTooLargeException`; usa `EXTRA_FOTOS_URL` + carga por ID desde Firestore (igual que MainActivity). v1.41 (vc23), commit `94ef478`, release `v1.41`.
- **Ronda 13 (2026-09-18)**: **Play Billing launchPurchaseFlow integrado** en MisPublicaciones y PerfilPropietario. Flujo completo: solicitud pendiente → launchPurchaseFlow(sku, callback) → onSuccess: activar destacado (isFeatured, featuredUntil, purchaseToken, orderId) / onError: limpiar solicitud. 3 SKUs (destacar_7d/15d/30d) tipo INAPP. Configuración Play Console pendiente (crear productos, track interno, testers). Android v1.27 (vc9), release `v1.27`.
- **Ronda 14 (2026-09-18)**: **Bottom Sheet minimizable** (peek 100dp + expandible). BottomSheetBehavior nativo con peekHeight=100dp, draggable, hideable=false, estado inicial COLLAPSED. FAB se mueve con onSlide. Android v1.28 (vc10), release `v1.28`.
- **Ronda 15 (2026-09-18)**: **Sheet expandible al 60%** (height 0.6*alto, onSlide actualiza padding del mapa) + **mapa se centra al seleccionar marcador** (animateCamera newLatLng) + **popup persistente** (idMarcadorSeleccionado se reaplica tras reconstruir marcadores en onCameraIdle → el info window ya no se oculta al instante). Android v1.29 (vc11), release `v1.29`.
- **Fix post-R9 (2026-09-18)**: `MisPublicaciones` click no abría ficha → iba a main. Causa: `PropiedadDetalleActivity` leía `EXTRA_FOTOS/EXTRA_FOTOS_URL` solo con `getStringArrayListExtra` pero callers pasaban `String[]`; añadida lectura dual + validación `propId` vacía con finish+toast. `MisPublicacionesActivity`: log+toast en click, card `isFocusable=true`. Version bump Android **v1.22 (vc4)**, release GitHub `v1.22`.
- **Ronda 16 (2026-09-18, instalado en Xiaomi, SIN probar por el padre)**: (1) **Fix "no me aparece el listado"** — el bottom sheet quedaba FUERA de pantalla (bounds `0,1515-800,2230` en ventana 800x1226) por (a) rollback a path render probado v1.28 (`fitToContents=false` + wrap_content + peek 100dp) con tope 60% vía altura del RecyclerView (`0.6*alto - 76dp`) y (b) hardening `sheet.post { state = STATE_COLLAPSED }` contra **SavedState viejo que sobrevive `install -r`** (por eso el padre lo veía sin listado al actualizar); verificado por píxeles tras uninstall+install limpio + re-aplicación del post. Trampa de debug: `dumpsys activity top` mezcla hierarchies de apps en background (¡la app de stickmans corre en este teléfono con su propio MainActivity!) → confirmar foco con `dumpsys window | findstr mCurrentFocus`; píxeles = verdad. (2) **Selector de ubicación en el mapa al publicar**: `MapaSeleccionActivity` (mapa fullscreen, tap coloca marcador, FAB mi ubicación con FusedLocation, Confirmar devuelve lat/lng) + botón "Elegir ubicacion en el mapa" en el alta (`RegistrarPropiedadActivity` launcher → set lat/lng + reverse-geocode con Geocoder + `tvPropUbicacionInfo`). Versión v1.30 (vc12), release `v1.30`. Tap-through pendiente de prueba del padre; Play Console sigue en verificación; iOS AlkilApp pendiente.
- **Ronda 17 (2026-09-18)**: **Bottom sheet MANUAL (adiós BottomSheetBehavior)** — el padre volvió a reportar listado invisible y en la resolución nueva del Xiaomi (**1080x2400 / density 450**, antes 800x1280/density 1.5; "nuevo dispositivo" = mismo serial, display reseteado) el sheet seguía sin verificar. Se ELIMINÓ la librería (su SavedState sobrevivía `install -r`): ahora el sheet es un `LinearLayout` con `layout_gravity="bottom"`, **altura fija 60% de pantalla**, minimizar = `translationY = alto - 100dp` (peek), expandir = `translationY = 0`; arrastre por la cabecera (OnTouchListener rawY + clamp) y **tap alterna expandir/minimizar**; `animarA()` con `setUpdateListener` mueve FAB + `mMap.setPadding` en vivo; peek sólido blanco (header y RecyclerView con `background=white`, antes transparente → se veía el mapa a través). Verificado por uiautomator: `bottomSheet.[0,2119][1080,2400]` = peek 100dp exacto, título "Inmuebles Cercanos" visible, tarjetas reales, strip blanco. Gotcha: `adb shell findstr` NO existe en el dispositivo (filtrar logcat host-side). Versión **v1.31 (vc13)**, commit `700d6c1`, release `v1.31`, instalada. Pendiente: prueba interactiva del padre (arrastrar/tocar el sheet, expandir 60% y minimizar) + tap-through del selector de mapa (v1.30, sin probar por nadie).
- **Ronda 18 (2026-09-18)**: **marcadores de inmuebles oscuros** (`ic_marker_inmueble.xml`, teardrop slate `#1E293B` centro blanco) en vez del rojo `HUE_RED`. **Gotcha**: `BitmapDescriptorFactory.fromResource` CRASHEA con vectores ("Failed to decode image... must be a Bitmap") → `cargarIconoRes()` rasteriza vector→Bitmap→`fromBitmap`. Verificado sin crash; el padre estaba tocando la app (abrió la ficha desde el mapa). Android v1.32 (vc14), commit `4f8084d`, release `v1.32`, instalada.
- **Ronda 19 (2026-09-18)**: en **Mis Publicaciones**, si el estado es **finalizado** ya NO aparece la opción "Destacar publicación" (`menu_destacar.isVisible = estadoNorm != "finalizado"`); en PerfilPropietario ya estaba bien (el botón vive dentro del `if esDisponible||esPausada`). Gotcha adb: dispositivo pasó a "offline" → `adb kill-server`+`start-server` lo restaura. Android v1.33 (vc15), commit `909e3a2`, release `v1.33`, instalada.
- [Project overview](project_overview.md) — what the stickman project is + its public GitHub repo URL
- [Gemini quota per project](gemini_quota_per_project.md) — why characters go silent, per-project key quota; corrected 2026-08-22, AQ.Ab8RN6... keys ARE valid
- [Windows dev workflow](windows_dev_workflow.md) — how to restart/rebuild locally (env reload, JDK for Android, adb path)
- [Feedback: workflow style](feedback_workflow_style.md) — terse multi-tasking messages, delegates execution, engages with clarifying questions
- [Android per-node model mode](android_multi_provider_mode.md) — WIP: each character/node can pick its own AI provider (gemini/openai/groq/openrouter), uncommitted as of 2026-08-16
- [Stick Nodes WASM renderer prototype](sn_proto_wasm_renderer.md) — scratch project (outside repo) parsing/rendering real .nodes rig files via WASM; head-circle + octagon-head bugs fixed 2026-08-16
- [Android rig renderer](android_rig_renderer.md) — Red now renders via procedural Kotlin rig (RigView/PoseLibrary) incl. wall-climbing; only Stand/Walk visually confirmed on-device so far
- [Android screen vision](android_screen_vision.md) — characters see phone screenshots via accessibility service every AI tick; had a real crash loop (fixed) — see this before assuming it "just works"
- [Android chat + movement fixes](android_chat_and_movement.md) — chat now a floating top-right button (no more app-switch on tap), and errors no longer trigger random walking
- [PC settings window](pc_settings_window.md) — pre-launch menu for provider/API keys/which characters appear; had to fix config.js getters + Shimeji's separate ActiveShimeji config
- [Sleep/tiredness system](sleep_tiredness_system.md) — auto-forced sleep (night + awake-duration) on Android and PC, not yet visually confirmed live (thresholds are long)
- [PC engine replacement](pc_engine_replacement.md) — Shimeji-ee fully replaced by native JS rig renderer windows (commit a57608d), drag via OS window drag, some caveats unverified live
- [Android idle sway + wander](android_idle_sway_and_repetition.md) — anti-repetition, idle sway, autonomous wander; "not responding" bug root-caused to invalid API key, not code
- [Installer stale, no window opens](installer_stale_no_window.md) — release/ installer built 2026-08-15 predates PC engine replacement, missing renderer files main.js needs; fix is npm run dist + reinstall
- [PC per-character AI provider](pc_per_character_provider.md) — ported Android's per-node provider picker to PC (config.js/pcSettings.js/settings UI); confirmed committed 2026-08-23
- [Custom character creator](custom_character_creator.md) — "crear tu propio stickman": clone Red/TCO rigs, face/gender/accessory, color picker, edit-existing; v2.4→v2.15/v1.4→v1.14
- [Attention focus + fluid animations + extras](attention_focus_fluidity_extras.md) — v2.19.0/v1.18: characters know mouse/touch position, global attention selector (camera vs mouse/touch), TICK_MS 40→25 (~40fps), PC exit button at startup, Android per-character private chat
- [Survival system + kitchen + fight](survival_system_kitchen_fight.md) — v2.20.0/v1.19: vida/hambre/sed bars, eating/drinking with visible food props, fighting that can kill, death + revive via chat, polygon rendering on Android
